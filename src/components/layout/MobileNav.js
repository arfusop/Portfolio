import React from 'react'

const MobileNav = () => {
    return (
        <nav id="mobileNav">
            mobile nav bar
            <button>yer</button>
        </nav>
    )
}

export default MobileNav
